import App from './App.tsx'
import ReactDOM from 'react-dom/client'
import './css/index.css' //The css file which is over everything

ReactDOM.createRoot(document.getElementById("root")!).render(<App />)